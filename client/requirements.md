## Packages
framer-motion | Essential for clean, minimal, premium micro-interactions and layout animations.

## Notes
- Images are processed locally using native FileReader to convert to base64 before sending to the prediction endpoint.
- Tailwind custom fonts are applied via CSS variables in the base layer to achieve the premium minimal aesthetic.
